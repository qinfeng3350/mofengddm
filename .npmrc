# pnpm 配置（仅 pnpm 使用，npm 会忽略这些配置）
# 提升所有依赖到 node_modules（解决 concurrently 找不到 yargs 等问题）
shamefully-hoist=true

# 使用国内镜像加速（可选）
# registry=https://registry.npmmirror.com

# 自动安装 peer dependencies
auto-install-peers=true

